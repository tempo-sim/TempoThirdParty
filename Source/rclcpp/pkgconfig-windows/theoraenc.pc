# theoraenc installed pkg-config file

prefix=${pcfiledir}/../install
exec_prefix=${prefix}
libdir=${prefix}/lib
includedir=${prefix}/include

Name: theora
Description: Theora video codec (encoder)
Version: 1.1.1
Requires: theoradec, ogg >= 1.1
Conflicts:
Libs: -L${libdir} -ltheora
Cflags: -I${includedir}

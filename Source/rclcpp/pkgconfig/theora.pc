# theora installed pkg-config file

prefix=${pcfiledir}/../install
exec_prefix=${prefix}
libdir=${prefix}/lib
includedir=${prefix}/include

Name: theora
Description: Theora video codec
Version: 1.1.1
Requires: ogg >= 1.1
Conflicts:
Libs: ${libdir}/libtheora.so
Cflags: -I${includedir}

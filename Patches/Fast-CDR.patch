diff --git a/include/fastcdr/Cdr.h b/include/fastcdr/Cdr.h
index a582e6a..3cd5216 100644
--- a/include/fastcdr/Cdr.h
+++ b/include/fastcdr/Cdr.h
@@ -685,9 +685,15 @@ public:
      * @exception exception::NotEnoughMemoryException This exception is thrown when trying to serialize a position that exceeds the internal memory size.
      * @exception exception::BadParamException This exception is thrown when trying to serialize a string with null characters.
      */
-    TEMPLATE_SPEC
+    // Tempo change: templated on the traits and allocator rather than specialized for std::string.
+    // ROS messages default to std::pmr::polymorphic_allocator, so the strings reaching here are
+    // std::pmr::string. Without this they fall through to the generic serialize(const _T&) member,
+    // which forwards to the free function template eprosima::fastcdr::serialize<_T>() -- declared
+    // at the top of this header but never defined -- and the failure surfaces as an unresolved
+    // external at link time in every generated type support library.
+    template<typename _Traits, typename _Allocator>
     Cdr& serialize(
-            const std::string& string_t)
+            const std::basic_string<char, _Traits, _Allocator>& string_t)
     {
         // Check there are no null characters in the string.
         const char* c_str = string_t.c_str();
@@ -706,9 +712,10 @@ public:
      * @return Reference to the eprosima::fastcdr::Cdr object.
      * @exception exception::NotEnoughMemoryException This exception is thrown when trying to serialize a position that exceeds the internal memory size.
      */
-    TEMPLATE_SPEC
+    // Tempo change: see the std::basic_string overload above.
+    template<typename _Traits, typename _Allocator>
     Cdr& serialize(
-            const std::wstring& string_t)
+            const std::basic_string<wchar_t, _Traits, _Allocator>& string_t)
     {
         return serialize(string_t.c_str());
     }
@@ -759,10 +766,10 @@ public:
      * @return Reference to the eprosima::fastcdr::Cdr object.
      * @exception exception::NotEnoughMemoryException This exception is thrown when trying to serialize a position that exceeds the internal memory size.
      */
-    template<class _T, typename std::enable_if<!std::is_enum<_T>::value &&
+    template<class _T, typename _Allocator, typename std::enable_if<!std::is_enum<_T>::value &&
             !std::is_arithmetic<_T>::value>::type* = nullptr>
     Cdr& serialize(
-            const std::vector<_T>& vector_t)
+            const std::vector<_T, _Allocator>& vector_t)
     {
         Cdr::state dheader_state {allocate_xcdrv2_dheader()};
 
@@ -789,10 +796,10 @@ public:
      * @return Reference to the eprosima::fastcdr::Cdr object.
      * @exception exception::NotEnoughMemoryException This exception is thrown when trying to serialize a position that exceeds the internal memory size.
      */
-    template<class _T, typename std::enable_if<std::is_enum<_T>::value ||
+    template<class _T, typename _Allocator, typename std::enable_if<std::is_enum<_T>::value ||
             std::is_arithmetic<_T>::value>::type* = nullptr>
     Cdr& serialize(
-            const std::vector<_T>& vector_t)
+            const std::vector<_T, _Allocator>& vector_t)
     {
         state state_before_error(*this);
 
@@ -822,11 +829,24 @@ public:
      * @return Reference to the eprosima::fastcdr::Cdr object.
      * @exception exception::NotEnoughMemoryException This exception is thrown when trying to serialize a position that exceeds the internal memory size.
      */
-    TEMPLATE_SPEC
+    // Tempo change: allocator-templated so std::pmr::vector<bool> resolves here rather than to the
+    // generic std::vector<_T, _Allocator> overload above, which calls data() -- something the
+    // bit-packed std::vector<bool> specialization does not have. The bool helpers are private,
+    // non-template members defined in Cdr.cpp and typed to the default allocator, so anything else
+    // has to be converted; if constexpr keeps the ordinary std::vector<bool> path copy-free.
+    template<typename _Allocator>
     Cdr& serialize(
-            const std::vector<bool>& vector_t)
+            const std::vector<bool, _Allocator>& vector_t)
     {
-        return serialize_bool_sequence(vector_t);
+        if constexpr (std::is_same<_Allocator, std::allocator<bool>>::value)
+        {
+            return serialize_bool_sequence(vector_t);
+        }
+        else
+        {
+            const std::vector<bool> converted(vector_t.begin(), vector_t.end());
+            return serialize_bool_sequence(converted);
+        }
     }
 
     /*!
@@ -1226,10 +1246,10 @@ public:
      * @exception exception::NotEnoughMemoryException This exception is thrown when trying to encode into a buffer
      * position that exceeds the internal memory size.
      */
-    template<class _T, typename std::enable_if<std::is_enum<_T>::value ||
+    template<class _T, typename _Allocator, typename std::enable_if<std::is_enum<_T>::value ||
             std::is_arithmetic<_T>::value>::type* = nullptr>
     Cdr& serialize_array(
-            const std::vector<_T>& value)
+            const std::vector<_T, _Allocator>& value)
     {
         serialize_array(value.data(), value.size());
 
@@ -1243,10 +1263,10 @@ public:
      * @exception exception::NotEnoughMemoryException This exception is thrown when trying to encode into a buffer
      * position that exceeds the internal memory size.
      */
-    template<class _T, typename std::enable_if<!std::is_enum<_T>::value &&
+    template<class _T, typename _Allocator, typename std::enable_if<!std::is_enum<_T>::value &&
             !std::is_arithmetic<_T>::value>::type* = nullptr>
     Cdr& serialize_array(
-            const std::vector<_T>& value)
+            const std::vector<_T, _Allocator>& value)
     {
         Cdr::state dheader_state {allocate_xcdrv2_dheader()};
 
@@ -1265,9 +1285,9 @@ public:
      * @exception exception::NotEnoughMemoryException This exception is thrown when trying to encode into a buffer
      * position that exceeds the internal memory size.
      */
-    template<class _T>
+    template<class _T, typename _Allocator>
     Cdr& serialize_array(
-            const std::vector<_T>& value,
+            const std::vector<_T, _Allocator>& value,
             Endianness endianness)
     {
         bool aux_swap = swap_bytes_;
@@ -1295,11 +1315,20 @@ public:
      * @exception exception::NotEnoughMemoryException This exception is thrown when trying to encode into a buffer
      * position that exceeds the internal memory size.
      */
-    TEMPLATE_SPEC
+    // Tempo change: see the std::vector<bool> serialize overload above.
+    template<typename _Allocator>
     Cdr& serialize_array(
-            const std::vector<bool>& value)
+            const std::vector<bool, _Allocator>& value)
     {
-        serialize_bool_array(value);
+        if constexpr (std::is_same<_Allocator, std::allocator<bool>>::value)
+        {
+            serialize_bool_array(value);
+        }
+        else
+        {
+            const std::vector<bool> converted(value.begin(), value.end());
+            serialize_bool_array(converted);
+        }
 
         return *this;
     }
@@ -1755,9 +1784,10 @@ public:
      * @return Reference to the eprosima::fastcdr::Cdr object.
      * @exception exception::NotEnoughMemoryException This exception is thrown when trying to deserialize a position that exceeds the internal memory size.
      */
-    TEMPLATE_SPEC
+    // Tempo change: see the std::basic_string serialize overload above.
+    template<typename _Traits, typename _Allocator>
     Cdr& deserialize(
-            std::string& string_t)
+            std::basic_string<char, _Traits, _Allocator>& string_t)
     {
         uint32_t length = 0;
         const char* str = read_string(length);
@@ -1771,12 +1801,15 @@ public:
      * @return Reference to the eprosima::fastcdr::Cdr object.
      * @exception exception::NotEnoughMemoryException This exception is thrown when trying to deserialize a position that exceeds the internal memory size.
      */
-    TEMPLATE_SPEC
+    // Tempo change: see the std::basic_string serialize overload above. read_wstring() returns a
+    // std::wstring, so assign() rather than operator= to convert into the caller's allocator.
+    template<typename _Traits, typename _Allocator>
     Cdr& deserialize(
-            std::wstring& string_t)
+            std::basic_string<wchar_t, _Traits, _Allocator>& string_t)
     {
         uint32_t length = 0;
-        string_t = read_wstring(length);
+        const std::wstring str = read_wstring(length);
+        string_t.assign(str.data(), str.size());
         return *this;
     }
 
@@ -1839,10 +1872,10 @@ public:
      * @return Reference to the eprosima::fastcdr::Cdr object.
      * @exception exception::NotEnoughMemoryException This exception is thrown when trying to deserialize a position that exceeds the internal memory size.
      */
-    template<class _T, typename std::enable_if<!std::is_enum<_T>::value &&
+    template<class _T, typename _Allocator, typename std::enable_if<!std::is_enum<_T>::value &&
             !std::is_arithmetic<_T>::value>::type* = nullptr>
     Cdr& deserialize(
-            std::vector<_T>& vector_t)
+            std::vector<_T, _Allocator>& vector_t)
     {
         uint32_t sequence_length {0};
 
@@ -1917,10 +1950,10 @@ public:
      * @return Reference to the eprosima::fastcdr::Cdr object.
      * @exception exception::NotEnoughMemoryException This exception is thrown when trying to deserialize a position that exceeds the internal memory size.
      */
-    template<class _T, typename std::enable_if<std::is_enum<_T>::value ||
+    template<class _T, typename _Allocator, typename std::enable_if<std::is_enum<_T>::value ||
             std::is_arithmetic<_T>::value>::type* = nullptr>
     Cdr& deserialize(
-            std::vector<_T>& vector_t)
+            std::vector<_T, _Allocator>& vector_t)
     {
         uint32_t sequence_length = 0;
         state state_before_error(*this);
@@ -1960,11 +1993,22 @@ public:
      * @return Reference to the eprosima::fastcdr::Cdr object.
      * @exception exception::NotEnoughMemoryException This exception is thrown when trying to deserialize a position that exceeds the internal memory size.
      */
-    TEMPLATE_SPEC
+    // Tempo change: see the std::vector<bool> serialize overload above.
+    template<typename _Allocator>
     Cdr& deserialize(
-            std::vector<bool>& vector_t)
+            std::vector<bool, _Allocator>& vector_t)
     {
-        return deserialize_bool_sequence(vector_t);
+        if constexpr (std::is_same<_Allocator, std::allocator<bool>>::value)
+        {
+            return deserialize_bool_sequence(vector_t);
+        }
+        else
+        {
+            std::vector<bool> converted;
+            deserialize_bool_sequence(converted);
+            vector_t.assign(converted.begin(), converted.end());
+            return *this;
+        }
     }
 
     /*!
@@ -2359,10 +2403,10 @@ public:
      * @exception exception::NotEnoughMemoryException This exception is thrown when trying to decode from a buffer
      * position that exceeds the internal memory size.
      */
-    template<class _T, typename std::enable_if<std::is_enum<_T>::value ||
+    template<class _T, typename _Allocator, typename std::enable_if<std::is_enum<_T>::value ||
             std::is_arithmetic<_T>::value>::type* = nullptr>
     Cdr& deserialize_array(
-            std::vector<_T>& value)
+            std::vector<_T, _Allocator>& value)
     {
         deserialize_array(value.data(), value.size());
 
@@ -2379,10 +2423,10 @@ public:
      * @exception exception::NotEnoughMemoryException This exception is thrown when trying to decode from a buffer
      * position that exceeds the internal memory size.
      */
-    template<class _T, typename std::enable_if<!std::is_enum<_T>::value &&
+    template<class _T, typename _Allocator, typename std::enable_if<!std::is_enum<_T>::value &&
             !std::is_arithmetic<_T>::value>::type* = nullptr>
     Cdr& deserialize_array(
-            std::vector<_T>& value)
+            std::vector<_T, _Allocator>& value)
     {
         if (CdrVersion::XCDRv2 == cdr_version_)
         {
@@ -2421,9 +2465,9 @@ public:
      * @exception exception::NotEnoughMemoryException This exception is thrown when trying to decode from a buffer
      * position that exceeds the internal memory size.
      */
-    template<class _T>
+    template<class _T, typename _Allocator>
     Cdr& deserialize_array(
-            std::vector<_T>& value,
+            std::vector<_T, _Allocator>& value,
             Endianness endianness)
     {
         bool aux_swap = swap_bytes_;
@@ -2454,11 +2498,23 @@ public:
      * @exception exception::NotEnoughMemoryException This exception is thrown when trying to encode into a buffer
      * position that exceeds the internal memory size.
      */
-    TEMPLATE_SPEC
+    // Tempo change: see the std::vector<bool> serialize overload above. deserialize_bool_array()
+    // fills in place and relies on the caller having sized the vector, so the temporary is sized
+    // to match before the call and copied back afterwards.
+    template<typename _Allocator>
     Cdr& deserialize_array(
-            std::vector<bool>& value)
+            std::vector<bool, _Allocator>& value)
     {
-        deserialize_bool_array(value);
+        if constexpr (std::is_same<_Allocator, std::allocator<bool>>::value)
+        {
+            deserialize_bool_array(value);
+        }
+        else
+        {
+            std::vector<bool> converted(value.size());
+            deserialize_bool_array(converted);
+            value.assign(converted.begin(), converted.end());
+        }
 
         return *this;
     }

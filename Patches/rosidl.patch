diff --git a/rosidl_generator_cpp/resource/idl__struct.hpp.em b/rosidl_generator_cpp/resource/idl__struct.hpp.em
index b8f98561..3eb39eaa 100644
--- a/rosidl_generator_cpp/resource/idl__struct.hpp.em
+++ b/rosidl_generator_cpp/resource/idl__struct.hpp.em
@@ -30,6 +30,7 @@ include_directives = set()
 #include <array>
 #include <cstdint>
 #include <memory>
+#include <memory_resource>
 #include <string>
 #include <vector>
 
diff --git a/rosidl_generator_cpp/resource/msg__struct.hpp.em b/rosidl_generator_cpp/resource/msg__struct.hpp.em
index 6e583888..26b1065c 100644
--- a/rosidl_generator_cpp/resource/msg__struct.hpp.em
+++ b/rosidl_generator_cpp/resource/msg__struct.hpp.em
@@ -359,9 +359,17 @@ u@
   }
 };  // struct @(message.structure.namespaced_type.name)_
 
-// alias to use template instance with default allocator
+// alias to use template instance with polymorphic allocator
+//
+// NOTE: This is a Tempo change, and it is the single change the rest of the Tempo patch set exists
+// to support. ROS advertises support for a templated allocator type, but several layers still
+// assume the allocator is memory-compatible with std::allocator: tf2, rosidl's type introspection,
+// rmw's serialization/deserialization wrappers, and the underlying DDS serialization libraries.
+// Implementing genuine allocator support throughout would be the real fix, but that is a large
+// change -- especially in the introspection layer -- so as a temporary solution we change the
+// default here, and everywhere else, to std::pmr::polymorphic_allocator.
 using @(message.structure.namespaced_type.name) =
-  @(message_typename)_<std::allocator<void>>;
+  @(message_typename)_<std::pmr::polymorphic_allocator<void>>;
 
 // constant definitions
 @[for c in message.constants]@
diff --git a/rosidl_runtime_cpp/include/rosidl_runtime_cpp/traits.hpp b/rosidl_runtime_cpp/include/rosidl_runtime_cpp/traits.hpp
index 5a4773c6..86cd964a 100644
--- a/rosidl_runtime_cpp/include/rosidl_runtime_cpp/traits.hpp
+++ b/rosidl_runtime_cpp/include/rosidl_runtime_cpp/traits.hpp
@@ -107,13 +107,19 @@ inline void value_to_yaml(int64_t value, std::ostream & out)
   out << value;
 }
 
-inline void value_to_yaml(const std::string & value, std::ostream & out)
-{
+// Tempo change: templated on the allocator rather than taking std::string, because generated
+// messages default to std::pmr::polymorphic_allocator and so hold std::pmr::string. std::string
+// still matches, so this replaces the original overload rather than adding to it.
+template<typename Allocator>
+inline void value_to_yaml(
+  const std::basic_string<char, std::char_traits<char>, Allocator> & value, std::ostream & out)
+{
+  using string_type = std::basic_string<char, std::char_traits<char>, Allocator>;
   out << "\"";
   size_t index = 0;
   while (index < value.size()) {
     size_t pos = value.find_first_of("\\\"", index);
-    if (pos == std::string::npos) {
+    if (pos == string_type::npos) {
       pos = value.size();
     }
     out.write(&value[index], static_cast<std::streamsize>(pos - index));
@@ -126,7 +132,11 @@ inline void value_to_yaml(const std::string & value, std::ostream & out)
   out << "\"";
 }
 
-inline void value_to_yaml(const std::u16string & value, std::ostream & out)
+// Tempo change: templated on the allocator, as for the char overload above.
+template<typename Allocator>
+inline void value_to_yaml(
+  const std::basic_string<char16_t, std::char_traits<char16_t>, Allocator> & value,
+  std::ostream & out)
 {
   out << "\"";
   std::wstring_convert<std::codecvt_utf8_utf16<char16_t>, char16_t> convert;
diff --git a/rosidl_typesupport_introspection_cpp/resource/msg__type_support.cpp.em b/rosidl_typesupport_introspection_cpp/resource/msg__type_support.cpp.em
index 392fc839..69c97dc3 100644
--- a/rosidl_typesupport_introspection_cpp/resource/msg__type_support.cpp.em
+++ b/rosidl_typesupport_introspection_cpp/resource/msg__type_support.cpp.em
@@ -22,6 +22,7 @@ include_base = '/'.join(include_parts)
 header_files = [
     'array',
     'cstddef',  # providing offsetof()
+    'memory_resource',  # Tempo: members are std::pmr containers
     'string',
     'vector',
     'rosidl_runtime_c/message_type_support_struct.h',
@@ -79,9 +80,9 @@ from rosidl_generator_cpp import  MSG_TYPE_TO_CPP
 if isinstance(member.type.value_type, BasicType):
     type_ = MSG_TYPE_TO_CPP[member.type.value_type.typename]
 elif isinstance(member.type.value_type, AbstractString):
-    type_ = 'std::string'
+    type_ = 'std::pmr::string'
 elif isinstance(member.type.value_type, AbstractWString):
-    type_ = 'std::u16string'
+    type_ = 'std::pmr::u16string'
 elif isinstance(member.type.value_type, NamespacedType):
     type_ = '::'.join(member.type.value_type.namespaced_name())
 }@
@@ -91,7 +92,7 @@ size_t size_function__@(message.structure.namespaced_type.name)__@(member.name)(
   (void)untyped_member;
   return @(member.type.size);
 @[    else]@
-  const auto * member = reinterpret_cast<const std::vector<@(type_)> *>(untyped_member);
+  const auto * member = reinterpret_cast<const std::pmr::vector<@(type_)> *>(untyped_member);
   return member->size();
 @[    end if]@
 }
@@ -104,7 +105,7 @@ const void * get_const_function__@(message.structure.namespaced_type.name)__@(me
     *reinterpret_cast<const std::array<@(type_), @(member.type.size)> *>(untyped_member);
 @[      else]@
   const auto & member =
-    *reinterpret_cast<const std::vector<@(type_)> *>(untyped_member);
+    *reinterpret_cast<const std::pmr::vector<@(type_)> *>(untyped_member);
 @[      end if]@
   return &member[index];
 }
@@ -116,7 +117,7 @@ void * get_function__@(message.structure.namespaced_type.name)__@(member.name)(v
     *reinterpret_cast<std::array<@(type_), @(member.type.size)> *>(untyped_member);
 @[      else]@
   auto & member =
-    *reinterpret_cast<std::vector<@(type_)> *>(untyped_member);
+    *reinterpret_cast<std::pmr::vector<@(type_)> *>(untyped_member);
 @[      end if]@
   return &member[index];
 }
@@ -142,7 +143,7 @@ void assign_function__@(message.structure.namespaced_type.name)__@(member.name)(
 void fetch_function__@(message.structure.namespaced_type.name)__@(member.name)(
   const void * untyped_member, size_t index, void * untyped_value)
 {
-  const auto & member = *reinterpret_cast<const std::vector<@(type_)> *>(untyped_member);
+  const auto & member = *reinterpret_cast<const std::pmr::vector<@(type_)> *>(untyped_member);
   auto & value = *reinterpret_cast<@(type_) *>(untyped_value);
   value = member[index];
 }
@@ -150,7 +151,7 @@ void fetch_function__@(message.structure.namespaced_type.name)__@(member.name)(
 void assign_function__@(message.structure.namespaced_type.name)__@(member.name)(
   void * untyped_member, size_t index, const void * untyped_value)
 {
-  auto & member = *reinterpret_cast<std::vector<@(type_)> *>(untyped_member);
+  auto & member = *reinterpret_cast<std::pmr::vector<@(type_)> *>(untyped_member);
   const auto & value = *reinterpret_cast<const @(type_) *>(untyped_value);
   member[index] = value;
 }
@@ -160,7 +161,7 @@ void assign_function__@(message.structure.namespaced_type.name)__@(member.name)(
 void resize_function__@(message.structure.namespaced_type.name)__@(member.name)(void * untyped_member, size_t size)
 {
   auto * member =
-    reinterpret_cast<std::vector<@(type_)> *>(untyped_member);
+    reinterpret_cast<std::pmr::vector<@(type_)> *>(untyped_member);
   member->resize(size);
 }
 

diff --git a/rcl/CMakeLists.txt b/rcl/CMakeLists.txt
index 7aa81e8..3d393c0 100644
--- a/rcl/CMakeLists.txt
+++ b/rcl/CMakeLists.txt
@@ -68,6 +68,20 @@ set(${PROJECT_NAME}_sources
 )
 
 add_library(${PROJECT_NAME} ${${PROJECT_NAME}_sources})
+
+set_target_properties(${PROJECT_NAME} PROPERTIES 
+    COMPILE_PDB_NAME ${PROJECT_NAME}
+    COMPILE_PDB_OUTPUT_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}
+    COMPILE_FLAGS "/Zi"
+    LINK_FLAGS "/DEBUG /OPT:REF /OPT:ICF"
+)
+
+target_compile_options(${PROJECT_NAME} PRIVATE $<$<CONFIG:Release>:/Od /Zi>)
+target_link_options(${PROJECT_NAME} PRIVATE $<$<CONFIG:Release>:/DEBUG /OPT:REF /OPT:ICF>)
+
+target_compile_options(${PROJECT_NAME} PRIVATE /Zi)
+target_link_options(${PROJECT_NAME} PRIVATE /DEBUG)
+
 target_include_directories(${PROJECT_NAME} PUBLIC
   "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>"
   "$<INSTALL_INTERFACE:include/${PROJECT_NAME}>")

diff --git a/sensor_msgs/include/sensor_msgs/impl/point_cloud2_iterator.hpp b/sensor_msgs/include/sensor_msgs/impl/point_cloud2_iterator.hpp
index f701e7c..60ac1a3 100644
--- a/sensor_msgs/include/sensor_msgs/impl/point_cloud2_iterator.hpp
+++ b/sensor_msgs/include/sensor_msgs/impl/point_cloud2_iterator.hpp
@@ -34,6 +34,7 @@
 
 #include <sensor_msgs/msg/point_cloud2.hpp>
 #include <cstdarg>
+#include <cstring>
 #include <sstream>
 #include <string>
 #include <vector>
@@ -368,10 +369,14 @@ int PointCloud2IteratorBase<T, TT, U, C, V>::set_field(
   is_bigendian_ = cloud_msg.is_bigendian;
   point_step_ = cloud_msg.point_step;
   // make sure the channel is valid
-  std::vector<sensor_msgs::msg::PointField>::const_iterator field_iter = cloud_msg.fields.begin(),
-    field_end =
-    cloud_msg.fields.end();
-  while ((field_iter != field_end) && (field_iter->name != field_name)) {
+  // Tempo change: cloud_msg.fields is a std::pmr::vector, so its const_iterator is not
+  // std::vector<PointField>::const_iterator -- deduce it. The field name is likewise a
+  // std::pmr::string with no operator!= against std::string, so compare the characters.
+  auto field_iter = cloud_msg.fields.begin();
+  auto field_end = cloud_msg.fields.end();
+  while ((field_iter != field_end) &&
+    (0 != strcmp(field_iter->name.c_str(), field_name.c_str())))
+  {
     ++field_iter;
   }
 

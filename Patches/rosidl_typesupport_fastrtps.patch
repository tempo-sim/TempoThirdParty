diff --git a/rosidl_typesupport_fastrtps_cpp/include/rosidl_typesupport_fastrtps_cpp/serialization_helpers.hpp b/rosidl_typesupport_fastrtps_cpp/include/rosidl_typesupport_fastrtps_cpp/serialization_helpers.hpp
index 0b70af5..411d5d6 100644
--- a/rosidl_typesupport_fastrtps_cpp/include/rosidl_typesupport_fastrtps_cpp/serialization_helpers.hpp
+++ b/rosidl_typesupport_fastrtps_cpp/include/rosidl_typesupport_fastrtps_cpp/serialization_helpers.hpp
@@ -27,9 +27,13 @@
 namespace rosidl_typesupport_fastrtps_cpp
 {
 
+// Tempo change: templated on the allocator. Generated messages default to
+// std::pmr::polymorphic_allocator, so wstring fields arrive as std::pmr::u16string rather than
+// std::u16string. std::u16string still matches, so this replaces the original overload.
+template<typename _Traits, typename _Allocator>
 inline void cdr_serialize(
   eprosima::fastcdr::Cdr & cdr,
-  const std::u16string & u16str)
+  const std::basic_string<char16_t, _Traits, _Allocator> & u16str)
 {
   if (u16str.size() > std::numeric_limits<uint32_t>::max()) {
     throw std::overflow_error("String length exceeds does not fit in CDR serialization format");
@@ -46,10 +50,13 @@ inline void cdr_serialize(
   }
 }
 
+// Tempo change: see cdr_serialize above.
+template<typename _Traits, typename _Allocator>
 inline bool cdr_deserialize(
   eprosima::fastcdr::Cdr & cdr,
-  std::u16string & u16str)
+  std::basic_string<char16_t, _Traits, _Allocator> & u16str)
 {
+  using u16string_type = std::basic_string<char16_t, _Traits, _Allocator>;
   uint32_t len;
   cdr >> len;
   try {
@@ -63,11 +70,11 @@ inline bool cdr_deserialize(
     // serialized values would fit in the destination type.
     uint32_t c;
     cdr >> c;
-    if (c > std::numeric_limits<std::u16string::value_type>::max()) {
+    if (c > std::numeric_limits<typename u16string_type::value_type>::max()) {
       throw eprosima::fastcdr::exception::BadParamException(
               "Character value exceeds maximum value");
     }
-    u16str[i] = static_cast<std::u16string::value_type>(c);
+    u16str[i] = static_cast<typename u16string_type::value_type>(c);
   }
 
   return true;

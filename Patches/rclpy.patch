diff --git a/rclpy/CMakeLists.txt b/rclpy/CMakeLists.txt
index eb48e4e..260209a 100644
--- a/rclpy/CMakeLists.txt
+++ b/rclpy/CMakeLists.txt
@@ -43,6 +43,15 @@ find_package(Python3 REQUIRED COMPONENTS Interpreter Development)
 find_package(pybind11_vendor REQUIRED)
 find_package(pybind11 REQUIRED)
 
+find_library(UTIL_LIBRARY NAMES util)
+if(NOT UTIL_LIBRARY)
+    message(FATAL_ERROR "libutil not found")
+endif()
+find_library(DL_LIBRARY NAMES dl)
+if(NOT DL_LIBRARY)
+    message(FATAL_ERROR "libdl not found")
+endif()
+
 # enables using the Python extensions from the build space for testing
 file(WRITE "${CMAKE_CURRENT_BINARY_DIR}/test_rclpy/__init__.py" "")
 
@@ -115,6 +124,8 @@ target_link_libraries(_rclpy_pybind11 PRIVATE
   rcpputils::rcpputils
   rcutils::rcutils
   rosidl_runtime_c::rosidl_runtime_c
+  ${UTIL_LIBRARY}
+  ${DL_LIBRARY}
 )
 configure_build_install_location(_rclpy_pybind11)
 
@@ -151,7 +162,7 @@ if(BUILD_TESTING)
   ament_add_gtest(test_python_allocator
     test/test_python_allocator.cpp)
   target_include_directories(test_python_allocator PRIVATE src/rclpy)
-  target_link_libraries(test_python_allocator pybind11::embed)
+  target_link_libraries(test_python_allocator pybind11::embed ${UTIL_LIBRARY} ${DL_LIBRARY})
 
   if(NOT _typesupport_impls STREQUAL "")
     # Run each test in its own pytest invocation to isolate any global state in rclpy
